# Archived Code

Code retired when the site became room-only (Spec 1, July 2026).
Kept for reference/restore; NOT part of the build.
Nothing in `src/` may import from here.

## How to restore

```bash
git mv _archive/<path> src/<path>
```
Then re-add its route and imports.
